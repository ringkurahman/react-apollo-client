{
  "name": "graphql-apollo-client",
  "version": "1.0.0",
  "description": "GArticle publish top news",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "server": "nodemon server/server.js",
    "client": "npm start --prefix client",
    "jsonServ": "json-server --watch db.json --port 3004",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run jsonServ\""
  },
  "author": "Ringku Rahman",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.16.1",
    "apollo-server-express": "^2.16.1",
    "axios": "^0.21.1",
    "concurrently": "^5.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
